import {writeFile,readFile} from 'node:fs/promises';
const raw=process.env.SITE_URL||process.env.CF_PAGES_URL;
if(raw){const url=new URL(raw);if(!['https:','http:'].includes(url.protocol))throw Error('SITE_URL must be an HTTP URL');const origin=url.origin;await writeFile('dist/sitemap.xml',`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"><url><loc>${origin}/</loc></url></urlset>`);await writeFile('dist/robots.txt',`User-agent: *\nAllow: /\nSitemap: ${origin}/sitemap.xml\n`);const html=await readFile('dist/index.html','utf8');await writeFile('dist/index.html',html.replace('</head>',`<link rel="canonical" href="${origin}/"/><meta property="og:url" content="${origin}/"/></head>`));}else{await writeFile('dist/sitemap.xml','<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"></urlset>');console.log('Set SITE_URL at deployment to populate sitemap and canonical URL.');}
