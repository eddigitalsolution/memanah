import {useState,useEffect} from 'react';
import {flushSync} from 'react-dom';
import {motion,MotionConfig,AnimatePresence,useReducedMotion} from 'framer-motion';
import {Target,ArrowUpRight,ArrowRight,Menu,X,MapPin,Clock,ShieldCheck,Users,MessageCircle,Check,Plus,Minus,MoveUpRight,Crosshair} from 'lucide-react';
import {siteData} from '../data/siteData';
import {whatsappUrl,bookingMessage,validateBooking} from '../utils/whatsapp';
import {Brand,WhatsApp,Reveal,Art,nav} from './Shared';
export default function Navbar(){const[open,setOpen]=useState(false),[compact,setCompact]=useState(false);useEffect(()=>{const onScroll=()=>setCompact(window.scrollY>40);const desktop=window.matchMedia('(min-width: 761px)');const onDesktop=()=>{if(desktop.matches)setOpen(false)};const onEscape=e=>{if(e.key==='Escape')setOpen(false)};onScroll();window.addEventListener('scroll',onScroll,{passive:true});window.addEventListener('keydown',onEscape);desktop.addEventListener('change',onDesktop);return()=>{window.removeEventListener('scroll',onScroll);window.removeEventListener('keydown',onEscape);desktop.removeEventListener('change',onDesktop)}},[]);return <header className={compact?'nav-shell compact':'nav-shell'}><nav className="nav wrap" aria-label="Navigasi utama"><Brand/><div className="desktop-nav">{nav.map(([name,id])=><a key={id} href={`#${id}`}>{name}</a>)}</div><a className="button red nav-cta" href="#daftar">Daftar sekarang<ArrowUpRight size={17}/></a><button className="menu-toggle" type="button" aria-label={open?'Tutup menu':'Buka menu'} aria-expanded={open} aria-controls="mobile-menu" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></nav><AnimatePresence>{open&&<motion.div id="mobile-menu" className="mobile-nav" initial={{height:0,opacity:0}} animate={{height:'auto',opacity:1}} exit={{height:0,opacity:0}}>{nav.map(([name,id])=><a key={id} href={`#${id}`} onClick={()=>setOpen(false)}>{name}</a>)}<a className="button red mobile-only-cta" href="#daftar" onClick={()=>setOpen(false)}>Daftar sekarang<ArrowUpRight size={17}/></a></motion.div>}</AnimatePresence></header>}
